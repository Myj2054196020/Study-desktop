好的，以下是为 Codex 编写的完整实施提示词。

---

# 📐 桌面端学习管理应用 — Codex 实施提示词

---

## 零、项目概述（给 Codex 的上下文）

你要构建一个**桌面端本地学习管理应用**。用户在浏览器中已有一个人学习工作台（React+TS+Vite+IndexedDB），现在需要升级为**独立桌面应用**，增加全局搜索、番茄钟、学习统计图表、知识图谱、章节管理等功能。

**技术路线**：Electron + React + TypeScript + Vite（复用现有前端架构，Electron 提供桌面壳和本地文件系统能力）。

---

## 一、项目初始化

### 步骤 1.1：创建 Electron + React 项目

在终端依次执行以下命令：

```bash
# 1. 创建项目目录
mkdir learning-desktop
cd learning-desktop

# 2. 初始化 package.json
npm init -y

# 3. 安装 Electron 和打包工具
npm install electron electron-builder --save-dev

# 4. 安装前端依赖（React + Vite + UI 库）
npm install react react-dom
npm install -D vite @vitejs/plugin-react typescript @types/react @types/react-dom

# 5. 安装功能依赖
npm install dexie katex lucide-react recharts cytoscape cytoscape-dagre cytoscape-popper

# 6. 安装 Electron 主进程依赖
npm install electron-store better-sqlite3

# 7. 安装开发工具
npm install -D concurrently cross-env electron-reload

# 8. 初始化 TypeScript 配置
npx tsc --init
```

> ⚠️ **风险项**：`better-sqlite3` 需要原生编译，Windows 上可能需要 `windows-build-tools`。如果安装失败，替代方案为 `electron-store`（JSON 文件存储）+ `lowdb`，不影响功能。

### 步骤 1.2：package.json scripts 配置

编辑 `package.json`，将 `scripts` 替换为：

```json
{
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"cross-env NODE_ENV=development electron .\"",
    "build": "vite build && tsc -p tsconfig.electron.json",
    "package": "vite build && tsc -p tsconfig.electron.json && electron-builder"
  }
}
```

---

## 二、文件目录结构

请按以下结构创建所有目录和文件（每个文件路径后标注其职责）：

```
learning-desktop/
├── package.json                    # 项目配置（已创建）
├── tsconfig.json                   # TypeScript 前端配置
├── tsconfig.electron.json          # TypeScript Electron 主进程配置
├── vite.config.ts                  # Vite 构建配置
├── electron-builder.yml            # 打包配置
│
├── electron/                       # Electron 主进程
│   ├── main.ts                     # [核心] 主进程入口，创建窗口、注册 IPC
│   ├── preload.ts                  # [核心] 预加载脚本，暴露安全 API
│   ├── ipc/                        # IPC 通信处理
│   │   ├── database.ts             # SQLite 数据库操作（CRUD）
│   │   ├── search.ts               # 全局搜索索引构建与查询
│   │   └── fileSystem.ts           # 文件导入导出（Obsidian 互操作）
│
├── src/                            # React 前端（渲染进程）
│   ├── main.tsx                    # React 入口
│   ├── App.tsx                     # 应用根组件，路由与布局
│   ├── index.css                   # 全局样式 + Tailwind（或自行设计）
│   ├── types/
│   │   └── index.ts                # 全部 TypeScript 类型定义
│   ├── lib/
│   │   ├── db.ts                   # 前端数据库桥接（调用 preload API）
│   │   ├── search.ts               # 搜索桥接
│   │   └── utils.ts                # 工具函数
│   ├── stores/
│   │   ├── AppContext.tsx           # 全局状态（主题、当前页面）
│   │   ├── ChapterContext.tsx       # 章节学习状态
│   │   ├── PomodoroContext.tsx      # 番茄钟计时器状态
│   │   └── SearchContext.tsx        # 搜索弹窗状态
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx          # 左侧导航栏
│   │   │   ├── AppShell.tsx         # 主布局壳
│   │   │   └── TitleBar.tsx         # 自定义标题栏（Electron 无边框窗口）
│   │   ├── chapters/
│   │   │   ├── ChapterList.tsx      # 课本章节列表 + 完成标记
│   │   │   └── ChapterDetail.tsx    # 章节详情与学习内容
│   │   ├── search/
│   │   │   └── SearchModal.tsx      # 全局搜索弹窗（快捷键唤起）
│   │   ├── pomodoro/
│   │   │   ├── PomodoroTimer.tsx    # 番茄钟计时器 UI
│   │   │   └── PomodoroHistory.tsx  # 专注历史记录
│   │   ├── stats/
│   │   │   └── StatsDashboard.tsx   # 学习统计图表（Recharts）
│   │   ├── graph/
│   │   │   └── KnowledgeGraph.tsx   # 知识图谱（Cytoscape.js）
│   │   └── ui/
│   │       ├── Button.tsx           # 通用按钮
│   │       ├── Input.tsx            # 通用输入框
│   │       ├── Modal.tsx            # 通用弹窗
│   │       └── Checkbox.tsx         # 章节完成复选框
│
├── assets/                         # 静态资源
│   └── icon.png                    # 应用图标（256x256）
│
└── data/                           # 默认数据（首次启动时加载）
    └── default-textbooks.json      # 示例课本章节目录
```

### 创建命令（按顺序执行）：

```bash
mkdir electron electron/ipc
mkdir src src/types src/lib src/stores
mkdir src/components src/components/layout src/components/chapters
mkdir src/components/search src/components/pomodoro
mkdir src/components/stats src/components/graph src/components/ui
mkdir assets data
```

---

## 三、核心文件编码指引（按顺序实现）

### 3.1 Electron 主进程

#### 文件：`electron/main.ts`

**职责**：创建 BrowserWindow、加载 Vite 开发服务器或打包后的 HTML。

**编码要点**：
- 创建 1200×800 的窗口，最小尺寸 900×600
- 开发环境加载 `http://localhost:5173`，生产环境加载 `dist/index.html`
- 注册全局快捷键 `Ctrl+Shift+F`（Windows/Linux）和 `Cmd+Shift+F`（Mac），触发搜索窗口
- 监听 `app.on('ready')` 创建窗口
- 设置 `webPreferences.preload` 指向 `preload.ts`
- 启用在 Electron 窗口标题栏显示 `{{title_text}}`

#### 文件：`electron/preload.ts`

**职责**：通过 `contextBridge` 向前端暴露安全的数据库和文件系统 API。

**编码要点**：
- 暴露 `window.electronAPI` 对象，包含以下方法：
  - `search(query: string): Promise<SearchResult[]>` — 全局搜索
  - `getChapters(): Promise<Chapter[]>` — 获取所有章节
  - `updateChapter(id, data): Promise<void>` — 更新章节完成状态
  - `getPomodoroHistory(): Promise<PomodoroRecord[]>` — 获取番茄钟历史
  - `savePomodoroRecord(record): Promise<void>` — 保存番茄钟记录
  - `getStats(): Promise<StudyStats>` — 获取学习统计数据
  - `exportData(): Promise<string>` — 导出全部数据为 JSON
  - `importData(json: string): Promise<void>` — 从 JSON 导入数据

#### 文件：`electron/ipc/database.ts`

**职责**：使用 better-sqlite3 管理本地 SQLite 数据库。

**编码要点**：
- 数据库文件路径：`app.getPath('userData')/learning.db`
- 建表语句（在首次启动时执行）：
  ```sql
  CREATE TABLE IF NOT EXISTS textbooks (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    subject TEXT,
    total_chapters INTEGER DEFAULT 0,
    created_at TEXT DEFAULT (datetime('now'))
  );
  
  CREATE TABLE IF NOT EXISTS chapters (
    id TEXT PRIMARY KEY,
    textbook_id TEXT,
    title TEXT NOT NULL,
    order_num INTEGER,
    content TEXT,           -- Markdown 学习内容
    completed INTEGER DEFAULT 0,
    completed_at TEXT,
    study_minutes INTEGER DEFAULT 0,
    tags TEXT,              -- JSON 数组字符串
    parent_id TEXT,         -- 父章节 ID（用于知识图谱连线）
    created_at TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (textbook_id) REFERENCES textbooks(id)
  );
  
  CREATE TABLE IF NOT EXISTS pomodoro_records (
    id TEXT PRIMARY KEY,
    start_time TEXT,
    end_time TEXT,
    duration_minutes INTEGER,
    chapter_id TEXT,
    completed INTEGER DEFAULT 0,
    FOREIGN KEY (chapter_id) REFERENCES chapters(id)
  );
  
  CREATE TABLE IF NOT EXISTS search_index (
    chapter_id TEXT PRIMARY KEY,
    title_tokens TEXT,
    content_tokens TEXT,
    FOREIGN KEY (chapter_id) REFERENCES chapters(id)
  );
  ```
- 封装 CRUD 方法：`insertChapter`, `updateChapter`, `deleteChapter`, `getAllChapters`, `markChapterComplete` 等

#### 文件：`electron/ipc/search.ts`

**职责**：构建和查询全文搜索索引。

**编码要点**：
- 章节创建/更新时，自动提取标题和内容的关键词（中文分词可用简单的 2-gram）
- 搜索结果按相关度排序（标题匹配权重 > 内容匹配权重）
- 返回结果包含：章节 ID、标题、匹配片段（高亮上下文 50 字）、所属课本名称

#### 文件：`electron/ipc/fileSystem.ts`

**职责**：处理文件导入导出。

**编码要点**：
- `exportAllData()`：将所有表数据导出为单个 JSON 文件，弹出保存对话框
- `importData()`：弹出打开文件对话框，解析 JSON 并覆盖数据库
- `exportChapterToMarkdown(chapterId)`：将章节内容导出为 `.md` 文件

---

### 3.2 TypeScript 类型定义

#### 文件：`src/types/index.ts`

**完整的类型定义**：

```typescript
export interface Textbook {
  id: string
  name: string
  subject: string
  totalChapters: number
  createdAt: string
}

export interface Chapter {
  id: string
  textbookId: string
  title: string
  order: number
  content: string          // Markdown
  completed: boolean
  completedAt?: string
  studyMinutes: number
  tags: string[]
  parentId?: string         // 图谱关联
  createdAt: string
}

export interface PomodoroRecord {
  id: string
  startTime: string
  endTime: string
  durationMinutes: number
  chapterId?: string
  completed: boolean
}

export interface SearchResult {
  chapterId: string
  title: string
  snippet: string           // 匹配片段
  textbookName: string
  score: number
}

export interface StudyStats {
  totalStudyMinutes: number
  completedChapters: number
  totalChapters: number
  dailyStats: { date: string; minutes: number; chapters: number }[]
  pomodoroCount: number
  streakDays: number
}

export type AppPage = 'chapters' | 'pomodoro' | 'stats' | 'graph'
```

---

### 3.3 前端状态管理

#### 文件：`src/stores/AppContext.tsx`

**编码要点**：
- 管理：`activePage: AppPage`、`theme: 'light' | 'dark'`、`searchOpen: boolean`
- 提供 `openSearch()` 和 `closeSearch()` 方法
- 通过 `window.electronAPI` 加载初始数据

#### 文件：`src/stores/ChapterContext.tsx`

**编码要点**：
- 管理：`chapters: Chapter[]`、`textbooks: Textbook[]`
- 方法：`toggleChapterComplete(id)`、`addChapter(data)`、`updateChapterContent(id, markdown)`
- toggle 时调用 `window.electronAPI.updateChapter()` 持久化 + 更新 search_index

#### 文件：`src/stores/PomodoroContext.tsx`

**编码要点**：
- 管理：`isRunning`、`timeLeft`（秒）、`totalSession`（25分钟=1500秒）、`sessionCount`
- 计时器逻辑：使用 `setInterval` 每秒递减，到 0 时自动记录并通知
- 方法：`start()`、`pause()`、`reset()`、`setDuration(minutes)`
- 每次完成时调用 `window.electronAPI.savePomodoroRecord()` 持久化

#### 文件：`src/stores/SearchContext.tsx`

**编码要点**：
- 管理：`query`、`results: SearchResult[]`、`isLoading`
- 方法：`search(query)` 调用 `window.electronAPI.search(query)` 并去抖 300ms

---

### 3.4 核心 UI 组件

#### 组件 3.4.1：`AppShell.tsx` — 应用主布局

**编码要点**：
- 左侧 Sidebar（宽度 220px）+ 右侧主内容区
- 顶部可选自定义标题栏（显示 `{{title_text}}`）
- 主内容区根据 `activePage` 切换显示不同模块
- 使用 `useHotkeys('mod+shift+f', openSearch)` 注册搜索快捷键

#### 组件 3.4.2：`Sidebar.tsx` — 侧边导航

**编码要点**：
- 导航项：📚 章节学习（chapters）、🍅 番茄钟（pomodoro）、📊 学习统计（stats）、🕸️ 知识图谱（graph）
- 高亮当前激活页面
- 底部显示应用标题 `{{title_text}}`
- 底部显示学习主题 `{{location_theme}}`

#### 组件 3.4.3：`ChapterList.tsx` — 章节列表与完成标记（核心需求 6）

**编码要点**：
- 左侧：课本章节树形列表（支持折叠/展开）
- 每个章节行包含：序号、标题、完成复选框、学习时长
- 点击复选框调用 `toggleChapterComplete(id)`：
  - 视觉反馈：划线 + 变灰 + 绿色勾
  - 数据同步：更新数据库 + 更新统计
- 点击章节标题进入 `ChapterDetail`
- 章节间若存在 `parentId` 关联，显示缩进或连线提示

#### 组件 3.4.4：`ChapterDetail.tsx` — 章节详情

**编码要点**：
- 上半部分：章节标题（可编辑）+ 标签（可编辑）
- 中间：Markdown 编辑器（支持 LaTeX 公式，使用 KaTeX）
- 下半部分：关联知识点列表（来自 `parentId` 关系）
- 底部：学习时长计时器 + "标记完成"按钮

#### 组件 3.4.5：`SearchModal.tsx` — 全局搜索弹窗（核心需求 2）

**编码要点**：
- 通过 `Ctrl+Shift+F`（或 `Cmd+Shift+F`）唤起
- 半透明遮罩 + 居中搜索框
- 输入框自动聚焦，实时搜索（300ms 去抖）
- 结果列表显示：章节标题、匹配片段（关键词高亮）、所属课本
- 点击结果跳转到对应章节
- 支持键盘导航（上下箭头 + Enter）
- 按 `Esc` 或点击遮罩关闭
- 空状态提示："输入关键词搜索章节、笔记..."

#### 组件 3.4.6：`PomodoroTimer.tsx` — 番茄钟（核心需求 3）

**编码要点**：
- 圆形进度条（SVG 实现）显示剩余时间
- 中央大字显示 `MM:SS`
- 三个按钮：开始/暂停、重置、跳過（标记未完成）
- 预设时长选择：25 分钟（标准）/ 15 分钟（短）/ 45 分钟（长）
- 开始后标题栏显示倒计时
- 完成时播放系统通知 + 自动记录到数据库
- 底部显示今日已完成的番茄数 🍅×3

#### 组件 3.4.7：`StatsDashboard.tsx` — 学习统计图表（核心需求 4）

**编码要点**：
- 使用 **Recharts** 库实现以下图表：
  - **折线图**（LineChart）：近 7 天每日学习时长趋势，X 轴日期/Y 轴分钟数
  - **柱状图**（BarChart）：近 7 天每日完成章节数
  - **环形图**（PieChart）：总章节完成率（完成/未完成）
  - **卡片组**：总学习时长（小时）、总番茄数、连续打卡天数、完成章节数
- 数据来源：`window.electronAPI.getStats()`
- 支持切换查看范围：近 7 天 / 近 30 天 / 全部

#### 组件 3.4.8：`KnowledgeGraph.tsx` — 知识图谱（核心需求 5）

**编码要点**：
- 使用 **Cytoscape.js** 绘制节点-连线图
- 节点 = 章节/知识点，大小与学习时长成正比
- 连线 = `parentId` 关系（前驱/后继知识依赖）
- 已完成章节节点 = 绿色，未完成 = 灰色，当前学习 = 蓝色高亮
- 交互：
  - 支持拖拽平移画布
  - 鼠标滚轮缩放
  - 点击节点弹出 Tooltip 显示章节标题、完成状态、学习时长
  - 双击节点跳转到章节详情
- 布局使用 dagre 算法（层级排列）或 cose 算法（力导向）
- 初始化数据：从数据库加载所有章节及其 parentId 关系

---

### 3.5 样式与主题

#### 文件：`src/index.css`

**编码要点**：
- 使用 CSS 变量定义亮色和暗色主题
- 通过 `[data-theme="dark"]` 选择器切换
- 图标使用 `lucide-react`（轻量线性图标）
- 整体风格参考 Notion / Linear 的简洁学术风
- 配色：中性灰底 + 蓝色强调色（`#3B82F6`）
- 字体：系统默认（`-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC'`）

---

### 3.6 默认数据

#### 文件：`data/default-textbooks.json`

**编码要点**：首次启动时，如果数据库为空，自动加载此文件作为示例数据。包含：

```json
[
  {
    "name": "数学分析",
    "subject": "数学",
    "chapters": [
      { "title": "第一章：实数与极限", "order": 1, "tags": ["基础", "极限"] },
      { "title": "第二章：连续函数", "order": 2, "tags": ["连续", "中值定理"], "parentId": "ch-1" },
      { "title": "第三章：导数与微分", "order": 3, "tags": ["导数", "微分"], "parentId": "ch-2" }
    ]
  },
  {
    "name": "高等代数",
    "subject": "数学",
    "chapters": [
      { "title": "第一章：多项式", "order": 1, "tags": ["多项式", "因式分解"] },
      { "title": "第二章：行列式", "order": 2, "tags": ["行列式", "矩阵"], "parentId": "ch-4" }
    ]
  }
]
```

> 章节标题中可嵌入占位符，如 `"当前主题：{{location_theme}}"`。

---

### 3.7 Vite 配置

#### 文件：`vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: './',  // Electron 需要相对路径
  build: {
    outDir: 'dist',
  },
})
```

#### 文件：`tsconfig.electron.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "outDir": "dist-electron",
    "rootDir": "electron",
    "strict": true,
    "esModuleInterop": true
  },
  "include": ["electron/**/*"]
}
```

---

## 四、运行与验证方式

### 步骤 4.1：启动开发模式

```bash
npm run dev
```

预期结果：
- 终端出现 Vite 开发服务器地址（`http://localhost:5173`）
- Electron 窗口自动打开，加载应用界面
- 左侧导航栏可见四个模块入口
- 按 `Ctrl+Shift+F` 唤起搜索弹窗

### 步骤 4.2：功能验收清单

逐一验证以下场景：

| # | 验证项 | 操作 | 预期结果 |
|---|---|---|---|
| 1 | 桌面启动 | 双击应用图标或 `npm start` | Electron 独立窗口打开，非浏览器 |
| 2 | 章节列表 | 点击侧边栏「📚 章节学习」 | 显示课本章节树形列表，含示例数据 |
| 3 | 完成标记 | 勾选一个章节的复选框 | 章节划线变灰，统计图表数据更新 |
| 4 | 搜索弹窗 | 按 `Ctrl+Shift+F` | 弹出搜索框，输入关键词有实时结果 |
| 5 | 搜索跳转 | 点击搜索结果 | 跳转到对应章节详情页 |
| 6 | 番茄钟 | 点击「🍅 番茄钟」→ 点击「开始」 | 圆形进度条倒计时，25分钟 |
| 7 | 番茄钟完成 | 等待计时结束 | 系统通知弹出，记录保存 |
| 8 | 统计图表 | 点击「📊 学习统计」 | 折线图+柱状图+环形图正常渲染 |
| 9 | 知识图谱 | 点击「🕸️ 知识图谱」 | 节点+连线显示，可拖拽/缩放 |
| 10 | 图谱节点点击 | 点击图谱中某节点 | Tooltip 显示章节详情 |
| 11 | 数据持久化 | 关闭应用→重新打开 | 上次数据完整保留 |
| 12 | 占位符 | 检查标题栏和侧边栏 | `{{title_text}}` 和 `{{location_theme}}` 原样保留 |

### 步骤 4.3：生产打包

```bash
npm run package
```

预期输出：`release/` 目录下生成 `.exe`（Windows）或 `.dmg`（macOS）安装包。

---

## 五、质量检查清单

请 Codex 完成后自检以下项目：

- [ ] `npm run dev` 启动无报错，Electron 窗口正常显示
- [ ] 四个导航模块（章节/番茄钟/统计/图谱）全部可见且可交互
- [ ] 全局搜索弹窗可唤起、可搜索、可跳转
- [ ] 番茄钟 25/15/45 分钟三档可切换，计时准确
- [ ] 统计图表使用真实数据渲染（非 hardcode 假数据）
- [ ] 知识图谱节点可拖拽、缩放、点击
- [ ] 章节打钩后数据持久化（关闭重开不丢失）
- [ ] `{{title_text}}` 和 `{{location_theme}}` 占位符原样保留在源码中
- [ ] 所有 IPC 通信正常（搜索、数据库读写）
- [ ] 暗色/亮色主题可切换（如已实现）

---

## 六、风险项与技术替代方案

| 风险项 | 说明 | 解决方案 |
|---|---|---|
| ⚠️ `better-sqlite3` 原生编译失败 | Windows 需要 MSBuild 工具链 | 替代方案：`electron-store`（JSON 文件）作为存储，将建表改为 JSON Schema 校验。搜索改为内存索引。 |
| ⚠️ Cytoscape.js 图谱大数据量性能 | 超过 200 个节点可能卡顿 | 限制首屏加载 100 个节点，超出部分按需加载 |
| ⚠️ Tauri 作为替代框架 | Rust 工具链学习成本高 | 如果需要更小的包体积，可将整个项目迁移到 Tauri。关键改动：`electron/main.ts` → `src-tauri/main.rs`，`preload.ts` → Tauri command。**迁移成本约 2-3 天**。 |
| ⚠️ 中文分词搜索效果差 | 简单 2-gram 可能精度不足 | 可接受 v1 用简单分词，v2 引入 `nodejieba` 或 `@pku-lab/jieba-wasm` |

---

## 七、给 Codex 的执行策略建议

1. **先跑通 Electron 壳**（窗口显示 + Vite 热更新），确保 `npm run dev` 能启动
2. **再搭前端骨架**（Sidebar + 页面路由），4 个模块用占位文字
3. **逐个实现模块**：章节管理 → 搜索弹窗 → 番茄钟 → 统计图表 → 知识图谱
4. **最后联调数据流**（IPC + SQLite），确保刷新不丢数据
5. **打包测试**

每个模块完成后，使用 `console.log` 验证数据流通，再进行下一个模块。

---

> 📌 **提示词版本**：v1.0  
> 📌 **目标平台**：Windows / macOS / Linux（Electron 跨平台）  
> 📌 **预计 Codex 迭代轮次**：15-20 轮（按 5 个步骤 × 3-4 轮调试）  
> 📌 **占位符**：`{{title_text}}` 和 `{{location_theme}}` 已在上述步骤中标注嵌入位置

---

```json
// 元信息（供 Codex 解析）
{
  "prompt_version": "1.0",
  "target_framework": "Electron + React + TypeScript + Vite",
  "estimated_files": 30,
  "key_dependencies": ["electron", "react", "better-sqlite3", "recharts", "cytoscape", "katex"],
  "placeholders": ["{{title_text}}", "{{location_theme}}"],
  "risk_items": ["better-sqlite3 原生编译", "Cytoscape 大数据性能", "Tauri 迁移成本"],
  "verification_mode": "npm run dev → 手动验收 12 项"
}
```