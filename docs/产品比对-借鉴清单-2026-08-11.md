# 产品比对 · 借鉴清单（Study desktop × 优秀产品）

> 日期：2026-08-11 ｜ 方法：按核心模块逐项拆解优秀产品 → 对照我们已做/未做 → 决定"借鉴什么、怎么本土化"。
> 原则不变：参考只学结构与机制，不抄大牌视觉；一切落到「深夜书房 × 小咕」的本土语境。
>
> **2026-08-11 修订**：删除白噪音（本地合成音质不达标且无可捆绑的高质合规音频）与专注呼吸引导（遮罩式圆形引导遮挡界面、阻碍操作）。番茄钟保留台灯点亮仪式感与任务绑定闭环。

---

## 一、番茄钟 / 专注

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| 番茄ToDo | 学霸模式（强制锁机）、数据周报、目标番茄数 | 中：数据周报已有雏形（学习统计），锁机不适配桌面 |
| 潮汐 | 白噪音 + 专注呼吸引导 + 情景氛围 | 低：曾借鉴"呼吸引导"，但遮罩式引导阻碍操作已移除；氛围感由台灯点亮承接 |
| Forest | "种树"隐喻 + 不看手机才能活 | 中：已采用"台灯点亮"隐喻，不重复种树 |
| 滴答清单 | 任务绑定番茄、专注回顾 | 高：**已做**（任务绑定 + 番茄复盘闭环） |

**借鉴决定**：~~专注开始加一次轻柔的"呼吸引导"~~（曾实现为纯 CSS 遮罩圆形，实测遮挡界面、阻碍操作，已移除）。仪式感改由「点亮台灯」动画与专注结束复盘承接。

---

## 二、笔记 / 章节

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| Notion | 块化编辑、模板库、数据库视图 | 中：模板库已做（12 分类 + 字段化）；块化编辑成本高，暂不引入 |
| Obsidian | 双链、图谱、本地 Markdown、反链 | 高：图谱已做；**反链/关联**可借鉴——章节详情显示"引用过它的心得/卡片" |
| 语雀 | 文档结构化、知识库目录 | 中：课本→章节的目录结构已有 |
| OneNote | 自由画布 + 手写 | 低：桌面画布手写成本高，不做 |

**借鉴决定**：① 章节详情加"被引用"区（关联心得/卡片/错题反链，Obsidian 反链思路）；② 模板库已达标，暂不加块编辑。

---

## 三、复习卡片 / 记忆

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| Anki | FSRS 调度、卡片字段、热力图、插件生态 | 高：FSRS 已用；到期曲线/复习健康已做；**卡片字段化（正反+附加字段）**可借鉴 |
| 墨墨背单词 | 复习上限激励、记忆曲线可视化 | 中：曲线已有；"今日复习进度"提示已隐含在卡片页 |
| RemNote | 笔记即卡片（inline 挖空） | 高：**章节内一键挖空生成卡片**是亮点，与"从章节生成卡片"结合 |
| Quizlet | 多种学习模式（配对/测试） | 中：可加"自测模式"（根据卡片生成小测） |

**借鉴决定**：① 复习页加"今日进度环"（今日已复习/目标）；② 章节内选中文本 → 一键"挖空成卡"（选中即生成填空卡）；③ 卡片加"错题/视频/心得来源"标签（已部分有 mistakeId/note）。

---

## 四、错题本

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| 猿题库 | 错题按知识点归类、同类题推送 | 中：错因聚类已做；**同类题推送**需题库数据，不做 |
| 作业帮错题本 | 拍照录入 + 打印 | 高：**已做**拍照/图片录入 + OCR；打印输出可加（导出错题 PDF） |
| 不背单词/百词斩 | 图片联想记忆 | 低：错题配图已支持，联想记忆是内容策略，不做 |

**借鉴决定**：① 错题导出 PDF/打印（复用导出 Markdown 通道）；② 已达标。

---

## 五、心得 / 日记

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| flomo | 随时随手记、回顾机制 | 高：**快捷记一条**（标题栏 + 号已有）+ 回顾（每日小结已做） |
| Day One | 时间线、日记回顾、照片 | 中：时间线已做；照片日记可加（心得配图） |
| 得到/小报童 | 摘要+行动清单 | 中：心得模板"行动启发"字段已有 |

**借鉴决定**：① 心得支持**配图**（复用错题图片压缩通道）；② 快捷记入口已达标。

---

## 六、全局搜索 / 快捷操作

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| Raycast / Alfred | 命令面板、分组结果、快捷键 | 高：**已做**分组 + Ctrl+K；可加"命令面板"（快速跳转/新建） |
| Notion Quick Find | 实时搜索 + 分类 | 高：已达标 |
| 滴答清单 | 全局快速添加 | 中：标题栏 + 号快捷菜单已有（记任务/心得/错题/卡片） |

**借鉴决定**：搜索加"命令模式"（输入 `>` 显示操作命令：新建任务/心得/卡片、跳转模块）——Raycast 思路本土化，成本 ★★。

---

## 七、知识图谱

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| Obsidian Graph | 过滤/分组/多视图 | 高：**已做**（分组着色/筛选/布局/预设） |
| Heptabase | 白板卡片、多上下文 | 中：多上下文复用（一卡多用）是远期 |
| 幕布/知犀 | 大纲↔思维导图切换 | 中：章节大纲模板已提供结构 |

**借鉴决定**：已达标；远期可加"图谱 → 白板"卡片拖拽（Heptabase），放远期。

---

## 八、吉祥物 / 陪伴（品牌差异点）

| 优秀产品 | 它做得好 | 借鉴价值 |
|---|---|---|
| Duolingo | 吉祥物绑行为、表情系统、提醒有"脾气" | 中：**小咕已绑行为**（复盘/打卡/场景卡）；表情系统可逐步加 |
| 扇贝/多邻国 | 打卡仪式、成就墙 | 中：连续打卡已有；**成就/里程碑**（如"灯已亮 7 晚"）可加文案层 |
| 潮汐 | 氛围场景（晨间/深夜） | 高：**深夜书房氛围已做**（台灯之光）；可加"晨间/深夜"场景切换文案 |

**借鉴决定**：① 小咕保持"不催不闹"，不做 Duolingo 式威胁提醒；② 加**里程碑文案**（连续 7/30 晚的灯语），成本 ★。

---

## 九、借鉴清单（汇总 · 按优先级）

| # | 借鉴项 | 来源 | 本土化 | 成本 | 状态 |
|---|---|---|---|---|---|
| 1 | 章节反链（被引用的心得/卡片/错题） | Obsidian | 章节详情"关联回看"区 | ★★ | ✅ 已落地 |
| 2 | ~~专注呼吸引导~~ | 潮汐 | ~~点亮台灯前静一静~~ | ★ | ❌ 已移除（遮罩阻碍操作，改为台灯点亮仪式感） |
| 3 | 今日复习进度环 | 墨墨/Anki | 卡片页顶部环 | ★★ | ✅ 已落地 |
| 4 | 章节内选中挖空成卡 | RemNote | 选中文本→生成填空卡 | ★★ | ✅ 已落地 |
| 5 | 搜索命令模式（> 开头） | Raycast | 中文命令：跳转 | ★★ | ✅ 已落地 |
| 6 | 心得配图 | Day One | 复用图片压缩通道 | ★★ | ✅ 已落地 |
| 7 | 里程碑文案（7/30 晚灯语） | 多邻国/扇贝 | "灯已亮 7 晚" | ★ | ✅ 已落地 |
| 8 | 错题导出/打印 | 作业帮 | 错题页「导出/打印」：当前筛选错题生成可打印 HTML（按状态分组、含答案/错因/章节），浏览器打开即可打印成 PDF | ★★ | ✅ 已落地 2026-08-11 |
| 9 | 自测模式 | Quizlet | 卡片页「自测」：根据卡片生成选择题，错题一键入错题本 | ★★★ | ✅ 已落地 2026-08-11 |
| 10 | 白板/多上下文 | Heptabase | 图谱→白板 | ★★★★ | ❌ 远期 |

**结论**：不跟风大而全，优先做"把已有模块串起来"的 7 项（1-7），每一项都落到「深夜书房 × 小咕」语境；8-10 视精力排期。
